name: Build and Publish PDF

on:
  push:
    paths:
      - '**.md'
      - '.github/workflows/**'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up GitHub Pages
        uses: actions/configure-pages@v4

      - name: Install Pandoc, XeLaTeX, and Fonts
        run: |
          sudo apt update
          sudo apt install -y pandoc texlive-xetex fonts-noto texlive-fonts-recommended texlive-latex-extra

      - name: Clean Markdown input
        run: |
          iconv -f utf-8 -t utf-8 -c "Mobile Servicing Training.md" > clean.md

      - name: Convert Markdown to PDF with multilingual fonts
        run: |
          pandoc "clean.md" -o training_output.pdf \
            --pdf-engine=xelatex \
            -V mainfont="Noto Sans" \
            -V CJKmainfont="Noto Serif Bengali" \
            -V monofont="Noto Sans Mono" \
            -V fontsize=14pt

      - name: Prepare docs directory
        run: |
          mkdir -p docs
          mv training_output.pdf docs/

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
